<%= form_for(@contact_group) do |f| %>
    <% if @contact_group.errors.any? %>
        <div id="error_explanation" class="alert alert-danger" role="alert">
          <h2><%= pluralize(@contact_group.errors.count, 'error') %> prohibited this contact_group from being saved:</h2>

          <ul>
            <% @contact_group.errors.full_messages.each do |message| %>
                <li><%= message.html_safe %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :label %>
      <%= f.text_field :label, { class: 'form-control',
                                 required: 'required',
                                 pattern: '.{1,20}',
                                 title: "#{(t 'contact_group_form.label_required')}
                                         #{(t 'contact_group_form.label_length')}"} %>
    </div>
    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_field :description, { class: 'form-control',
                                       pattern: '.{0,120}',
                                       title: (t 'contact_group_form.description_length') } %>
    </div>
    <div class="form-group">
      <%= f.submit class: 'btn btn-primary' %>
    </div>
<% end %>
